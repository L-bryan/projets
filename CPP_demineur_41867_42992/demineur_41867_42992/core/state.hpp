#ifndef STATE_H
#define STATE_H

/**
 * @brief The State enum
 */
enum State{
    /**
     * the state is marked
     */
    marked,
    /**
     * the state is discover
     */
    discover,
    /**
     * the state is hidden
     */
    hidden
};

#endif // STATE_H
